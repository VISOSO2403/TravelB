import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  walletCard: {
    width: '100%',
    height: 100,
    backgroundColor: 'white',
    borderRadius: 10,
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
    marginBottom: 10,
  },
});
